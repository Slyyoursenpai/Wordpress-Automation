name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'

      - name: Install Maven
        uses: actions/setup-java@v3
        with:
          distribution: 'maven'
          maven-version: '3.8.6'

      - name: Build and Test
        run: mvn clean install

      - name: Run Tests
        run: mvn test
